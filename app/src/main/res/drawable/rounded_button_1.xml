<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Фон кнопки -->
    <solid android:color="@color/dark_gray" /> <!-- Цвет кнопки -->

    <!-- Закругленные углы -->
    <corners android:radius="16dp" />

    <!-- Отступы внутри кнопки -->
    <padding
        android:left="16dp"
        android:top="8dp"
        android:right="16dp"
        android:bottom="8dp" />
</shape>
